apiVersion: 1

providers:
  - name: 'fleet-dashboards'
    orgId: 1
    folder: 'Fleet Management'
    folderUid: 'fleet-folder'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
